# 자동화된 콘텐츠 파이프라인
## Automated Content Pipeline for Longevity Knowledge Platform

권위 기반의 '지식 구독 & AI 콘텐츠' 플랫폼을 위한 자동화 시스템

---

## 시스템 구조

```
┌─────────────────────────────────────────────────────────────┐
│                      Weekly Pipeline                        │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│   📥 논문 수집          📄 문서 처리         ✍️ 콘텐츠 생성     │
│   ───────────         ───────────         ───────────       │
│   • PubMed API        • Docling (PDF)     • Gemini 2.0      │
│   • RSS Feeds         • 표/그림 추출        • GPT-4o          │
│   • 관련성 점수화      • Markdown 변환      • 한국어 최적화    │
│                                                             │
│   ✓ 팩트 체크          📝 검토 대기         📤 발행            │
│   ───────────         ───────────         ───────────       │
│   • GPT-4 검증        • JSON 저장          • 뉴스레터         │
│   • 정확도 점수        • 교수님 검토        • 블로그           │
│   • 수정 제안          • 5-10분            • 유튜브           │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

---

## 설치

```bash
# 필수 패키지
pip install aiohttp google-generativeai openai docling

# 환경 변수 설정
export GEMINI_API_KEY="your-gemini-api-key"
export OPENAI_API_KEY="your-openai-api-key"
```

---

## 사용법

### 1. 주간 파이프라인 실행

```python
from content_pipeline import ContentPipeline
import asyncio

async def main():
    pipeline = ContentPipeline(ai_provider="gemini")
    drafts = await pipeline.run_weekly_pipeline()
    pipeline.save_drafts(drafts)

asyncio.run(main())
```

### 1-1. 빠른 시작 테스트 (논문 검색 + 콘텐츠 생성 + 팩트체크)

```bash
cd automation
python3 quick_start.py
```

### 2. 단일 논문 콘텐츠 생성

```python
from content_pipeline import Paper, ContentPipeline

paper = Paper(
    title="NAD+ supplementation extends healthspan in aged mice",
    authors=["Kim J", "Lee S"],
    abstract="...",
    journal="Nature Aging",
    doi="10.1038/...",
    pub_date="2024",
    url="https://..."
)

pipeline = ContentPipeline()
draft = await pipeline.generate_single_content(paper, content_type="newsletter")
```

### 3. PDF 논문 처리

```python
from content_pipeline import DocumentProcessor

processor = DocumentProcessor()
result = await processor.process_pdf("paper.pdf")
print(result["markdown"])
```

### 4. 인스타그램 카드뉴스 생성 (4장 캐러셀)

전제 조건:
- `Pillow` 설치: `pip install -r requirements.txt`
- 폰트 파일 준비: `automation/fonts/` 아래에 NotoSansKR 폰트 3개가 있어야 합니다.

```bash
cd automation
python3 card_news_generator.py
```

---

## 콘텐츠 유형

| Type | 길이 | 용도 |
|------|------|------|
| `newsletter` | 400-600자 | 주간 이메일 뉴스레터 |
| `blog` | 800-1200자 | 블로그/브런치 게시글 |
| `youtube_script` | 5-7분 | 유튜브 대본 |
| `vod_lecture` | 15-20분 | VOD 강의 스크립트 |

---

## 워크플로우 자동화 (Make.com / n8n)

### Make.com 시나리오

```
1. Schedule: 매주 월요일 오전 9시
2. HTTP Request: PubMed API 호출
3. Iterator: 각 논문에 대해
4. HTTP Request: Gemini API 호출 (콘텐츠 생성)
5. HTTP Request: GPT-4 API 호출 (팩트체크)
6. Google Sheets: 결과 저장
7. Email: 검토 요청 알림
```

### 예상 비용

| 항목 | 월간 예상 |
|------|----------|
| Gemini API | $10-20 |
| GPT-4 (팩트체크) | $5-10 |
| Make.com | $9-16 |
| **총합** | **$24-46** |

---

## 왜 커스텀 LLM이 필요 없는가?

### 비교

| 접근 방식 | 장점 | 단점 |
|-----------|------|------|
| **커스텀 LLM 훈련** | 완전한 제어 | 수백만원 비용, 수개월 개발, 한국어 데이터 부족 |
| **Gemini/GPT API** | 즉시 사용, 우수한 한국어, 저비용 | API 의존성 |

### 결론

귀하의 사용 사례(논문 → 대중적 콘텐츠 변환)에서:
- **Gemini 2.0 Flash**: 가장 빠르고 저렴, 한국어 우수
- **GPT-4o**: 최고 품질, 팩트체크에 적합
- **Claude**: 긴 문서 처리에 강점

이 세 가지를 조합하는 것이 커스텀 LLM 개발보다 **100배 이상 비용 효율적**입니다.

---

## 다음 단계

1. [ ] Make.com 계정 생성 및 시나리오 설정
2. [ ] Inoreader에 관련 저널 RSS 피드 등록
3. [ ] 첫 주간 파이프라인 테스트
4. [ ] 콘텐츠 템플릿 커스터마이징
5. [ ] Substack/Stibee 뉴스레터 연동

---

## 라이선스 확인

| 도구 | 라이선스 | 상업적 사용 |
|------|----------|------------|
| Docling | MIT | ✅ 무료 |
| Gemini API | 상용 | ✅ 유료 |
| GPT-4 API | 상용 | ✅ 유료 |
| PubMed API | 공개 | ✅ 무료 |
